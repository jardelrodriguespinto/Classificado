import NavbarPersonalizada from "./NavbarPersonalizada";
import '../css/navbarpersonalizada.css'

//header será diferente quando o usuario for diferente

function Header() {
  return (
    <header>
        <NavbarPersonalizada/>        
    </header>
  );
}

export default Header;